let initialState = {
    korzina: [
        ]       
    }


const korzinaReducer =(state = initialState,action)=> {
    switch (action.type) {
        // case 'CHOOSE_GOOD': {
        //     goods:{
                
        //     }
        // }
        default:
            return state
    }
}
export default korzinaReducer